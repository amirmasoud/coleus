query FetchUserByUsername($username: String) {
  user(username: $username) {
    id,
    name,
    username,
    photo_url,
    xsmall,
    small,
    placeholder,
    bio,
    books {
      id,
      title,
      description,
      cover,
      placeholder,
      slug,
      start
    }
  }
}
