...

1. SSL certificates -> Let's encrypt
2. Create database -> ./coleus psql -> create database coleus
3. provide keys for services

.env -> root
    |-> backend
    |-> frontend

importData.php --> >return 'https://image.coleus.app'<<

storage -> mkdir public
docker pull minio/mc
docker run --name coleus-storage-cli -it --entrypoint=/bin/sh --network=coleus_backend-network minio/mc
mc config host add coleus http://storage:9000
docker start -ia coleus-storage-cli
mc mb coleus/public
mc policy set public coleus/public

backend -> composer install && php artisan key:generate && php artisan migrate && php artisan imprt:data
Hasura -> export schema
ISSUES:
-1. chmod +x ./coleus
00. ./coleus is not working
01. Install docker and docker-compose automatically if not installed already (as non-root user)
02. Create database
03. Generate keys (.env)
04. Docker up the thing.
Change domains
install nginx for open 80 port to validate let's encrypt
Edit init-letecncrypt file for each domain

Add Acme challenge to DNS Records

Server
1. t3a.small -> 7.31 USD/month

cloudflare.ini
